<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, id: :post_title, placeholder: "Title", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.text_field :author, id: :post_author, placeholder: "Author", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.text_area :blog_entry, id: :post_blog_entry, placeholder: "Enter Post Here", class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
